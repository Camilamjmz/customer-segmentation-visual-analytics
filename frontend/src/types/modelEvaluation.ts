export interface ComparisonItem { algorithm:string; configuration:string; silhouette_score:number; davies_bouldin:number; calinski_harabasz:number; coverage_percentage:number; cluster_count:number; noise_percentage:number }
export interface ValidationCandidate { k:number; initialization_stability:{mean_pairwise_ari:number}; subsampling_stability:{iterations:number;sample_proportion:number;mean_ari:number;standard_deviation_ari:number;minimum_ari:number;maximum_ari:number;mean_cluster_percentage_variation:number;mean_centroid_distance:number}; feature_sensitivity:{alternative_feature:string;ari_vs_primary:number;primary_silhouette:number;alternative_silhouette:number}; internal_quality:{silhouette_score:number;davies_bouldin:number;calinski_harabasz:number}; cluster_balance:{smallest_cluster_percentage:number;largest_cluster_percentage:number}; customer_coverage_percentage:number; parsimony_note:string }
export interface ValidationResponse { candidates:ValidationCandidate[]; recommended_k:number; recommendation_rationale:string }
